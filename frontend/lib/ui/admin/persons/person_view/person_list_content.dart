import 'package:flutter/material.dart';

class PersonListContent extends StatelessWidget {
  const PersonListContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
