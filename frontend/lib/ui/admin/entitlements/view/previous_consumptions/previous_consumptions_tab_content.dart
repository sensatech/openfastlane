import 'package:flutter/material.dart';

class PreviousConsumptionsTabContent extends StatelessWidget {
  const PreviousConsumptionsTabContent({super.key, required this.entitlementCauseId});

  final String entitlementCauseId;

  @override
  Widget build(BuildContext context) {
    return Placeholder();
  }
}
